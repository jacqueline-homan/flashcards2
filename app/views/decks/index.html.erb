<b>~~Welcome~~</b><br />
<br />
<b><i><%= link_to "Add New Deck", new_deck_path %></i></b><br />
<br />
<% @decks.each do |deck| %>
  <div><%= link_to deck.name, deck_path(deck.id) %>
  	-
  	<%= link_to "Edit", edit_deck_path(deck.id) %>
  	<%= link_to "Delete", deck_path(deck.id), method: :delete, confirm: "Are you sure?" %>
  	</div>
<% end %>